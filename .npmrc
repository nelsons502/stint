# This project requires install scripts to run so that Electron's binary is
# downloaded and native modules (e.g. better-sqlite3) are rebuilt against
# Electron's Node ABI. This overrides a global `ignore-scripts=true` setting
# if you have one. See README for context.
ignore-scripts=false
